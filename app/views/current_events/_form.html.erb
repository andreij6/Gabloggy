<%= form_for(@current_event) do |f| %>
  <% if @current_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@current_event.errors.count, "error") %> prohibited this current_event from being saved:</h2>

      <ul>
      <% @current_event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_field :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
